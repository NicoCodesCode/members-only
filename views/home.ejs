<!DOCTYPE html>
<html lang="en">
  <%- include("partials/head") %>
  <body>
    <h1><%= title %></h1>
    <% if (locals.currentUser) { %>
    <h2>Welcome Again, <%= currentUser.username %></h2>
    <% } else { %>
    <a href="/sign-up">Sign Up</a>
    <a href="/log-in">Log In</a>
    <% } %>
  </body>
</html>
